import { Typography } from '@material-ui/core';
import React from 'react';
import {Component} from 'react';

export default class HomePage extends Component{
    render() {
        return <Typography>POGGERS</Typography>
    }
}